{
  "name": "easypos-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise-grade POS System for Computer Parts Shop",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "node scripts/init-dev.js",
    "dev": "npm run setup && concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\" \"node scripts/start-tunnel.js\"",
    "build": "npm run build --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
